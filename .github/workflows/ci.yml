name: CI

on:
  push:
    branches: [master, main]
  pull_request:

permissions: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI workflow for CodeQL"
